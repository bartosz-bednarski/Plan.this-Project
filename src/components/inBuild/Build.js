import classes from "./Build.module.css";
const Build = () => {
  return (
    <div className={classes["build-container"]}>
      <span>Build in progress!</span>
    </div>
  );
};

export default Build;
